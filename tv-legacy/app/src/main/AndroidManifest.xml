<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Ignored on older devices; required for Android 9+ foreground services. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- TV/box devices might not have a touchscreen. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.LinPlayerTvLegacy">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ServersActivity"
            android:exported="false" />

        <activity
            android:name=".ServerEditActivity"
            android:exported="false" />

        <activity
            android:name=".ShowDetailActivity"
            android:exported="false" />

        <activity
            android:name=".EpisodeListActivity"
            android:exported="false" />

        <activity
            android:name=".EpisodeDetailActivity"
            android:exported="false" />

        <activity
            android:name=".PlayerActivity"
            android:exported="false" />

        <service
            android:name=".ProxyService"
            android:exported="false" />
    </application>
</manifest>
