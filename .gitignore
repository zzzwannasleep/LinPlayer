# =============================================================================
# LinPlayer —— Rust(核心) + React/TS(各端 UI) + Tauri(桌面壳)
# 2026-07-19 重构:Flutter/Dart/苹果全线已删,原 native-poc/ 提到仓库根。
# Flutter 时代的忽略规则一并清空 —— 留着只会掩护下一次误提。
# 需要旧规则时:git show flutter-final:.gitignore
# =============================================================================

# --- 构建产物 / 依赖 ---------------------------------------------------------
node_modules/
dist/
dist-ssr/
# 绿色包产物(scripts/pack-portable.ps1 的输出):exe + 114MB libmpv + zip,共 240MB+。
# `dist` 只匹配同名条目,匹配不到这个目录 —— 自家脚本产出的东西必须自家忽略掉。
dist-portable/
*.local
packaging/

# libmpv 大二进制(117MB dll):本地构建输入,不入普通库;CI 现拉(见 .github/workflows/build.yml)
third_party/libmpv/*.dll
# 安卓侧同理:scripts/fetch-libmpv-android.sh 现拉,每个 ABI 一份 16~20MB
third_party/libmpv/android/
# 安卓的 native 产物(liblpcore.so 由 build-core-android.sh 编、libmpv.so 由 fetch 脚本拷)
apps/android/app/src/main/jniLibs/
# 安卓工程的构建产物与本地配置
apps/android/build/
apps/android/app/build/
apps/android/.gradle/
apps/android/local.properties
apps/android/keystore.properties

# 绿色包运行时数据(exe 同级 userdata/);跑 dev 构建时会出现在 target/ 下,别入库
userdata/

# --- 本地工作材料(用户 2026-07-15 明确不入库)---------------------------------
docs/desktop-drafts.html
docs/tv-drafts.html
docs/login-drafts.html
scripts/pack-portable.ps1

# --- SPIKE 的语料与产物(脚本现造,不入库)-------------------------------------
# clip*.mp4 由 spikes/s1-2/mkclip.py 现造(4K60 那个 141MB);out/ 是截图与报告
docs/go-migration/spikes/*.mp4
docs/go-migration/spikes/s1-2/*.mp4
docs/go-migration/spikes/s1-2/out/
docs/go-migration/spikes/s1-2/AvaloniaProbe/bin/
docs/go-migration/spikes/s1-2/AvaloniaProbe/obj/
docs/go-migration/spikes/s2/out/
tools/corecheck/bin/
tools/corecheck/obj/
docs/go-migration/spikes/s2/*.txt
docs/go-migration/spikes/s3/*.exe

# --- 项目级工具链(scripts/fetch-toolchain.sh 现拉,版本与 sha256 钉在脚本里)-------
# Go + zig + 各自的缓存,700MB 起步。换机器重跑 fetch 脚本,不入库。
.toolchain/

# 核心层产物(scripts/build-core.sh 的输出)
build/

# --- 密钥 / 凭据 -------------------------------------------------------------
*.env
*.env.local
*.env.*.local
.envrc
*.keystore
*.jks
*.p12
*.key
*.pem
key.properties
# OAuth 凭据原文(仅本地参考,绝不提交)
Trakt.txt
Bangumi.txt
# Cloudflare 代理本地环境变量(含 secret,绝不提交)
oauth-proxy/.dev.vars
oauth-proxy/.wrangler/
# Ani-rss API reference dumps(含私有服务器地址,绝不提交)
api-docs.json

# --- 日志 / 临时 -------------------------------------------------------------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
linplayer_log_*.txt
.tmp/
ScreenShot_*.png

# --- 打包产物(安卓端将来会有)-----------------------------------------------
*.apk
# *.apk 匹配不到 bundletool 产出的 .apks(APK Set),漏一个字母就够 50MB 裸奔在 git status 里
*.apks
*.aab
*.dex

# --- 编辑器 / 工具 -----------------------------------------------------------
.vscode/
.idea/
.cursor/
.zed/
.fleet/
.run/
.codegraph/
.omo/
*.iml
*.ipr
*.iws
*.suo
*.sw?
*.sln
.DS_Store
.history

# Agent / Skill 运行时文件
.agents/
skills-lock.json
.claude/skills/
.claude/settings.local.json
.claude/worktrees/
.claude/plans/

# --- 合并/备份残留 -----------------------------------------------------------
*.orig
*.rej

# --- 安卓产物与签名材料 -------------------------------------------------------
# 安卓端 2026-09-04 随 Rust 栈一起删除,重建时这几条仍然要挡住 ——
# keystore / jks 是签名私钥,按全局红线**永远**不进版本库。
**/*.apk
**/*.aab
**/*.keystore
**/*.jks

# 资源站接口地址清单(vod.json 之类)。里面是一串站点域名 —— 按全局红线,
# 这类东西不进版本库,插件也不内置它:VOD 资源站插件只留一个地址输入框,用户自己填。
vod.json

# 绑定层的编译产物(Commands.g.cs / .kt 本身是要进仓库的,它们是生成的**源码**)
bindings/csharp/bin/
bindings/csharp/obj/
bindings/kotlin/build/
bindings/kotlin/.gradle/
build/kotlin-check/

# Avalonia 桌面端的编译产物
apps/windows/**/bin/
apps/windows/**/obj/

# 自检 / 冒烟用的临时可执行文件(scripts/selfcheck-win.sh 会在 core/ 下兜底编一份)
core/*.exe

# --- 逆向细节文档(2026-09-04 用户拍板:仓库转公开,这批不进库)---------------
# 里面是 189 RSA/AES-HMAC、139 Authorization 自算、飞牛 authx 签名、115 m115 模幂、
# 夸克 Cookie 轮换的完整算法与端点。公开出去等于发布一本网盘逆向手册,
# 而且 lessons/sources.md 里还记着「某实例 IP 应视为已泄露」这类事故细节。
# ★ 文件本身留在本地磁盘(CLAUDE.md §4 要求 agent 能 grep 到),只是不进版本控制。
docs/lessons/sources.md
docs/go-migration/knowledge/SOURCES.md
docs/code-lore.md

# 手机端草稿(与 desktop-drafts / tv-drafts 同性质:本地工作材料,不入库)
docs/mobile-drafts/
