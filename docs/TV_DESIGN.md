# LinPlayer TV 端设计文档

> **⚠️ 读之前先看这段（2026-07-26）**
>
> 本文写于 Flutter 时代。TV 端 UI 已于 2026-07 用 **React + TypeScript** 重建（`ui/tv/`，
> 焦点走 norigin-spatial-navigation），壳是 `apps/android/`（Tauri）。
>
> - **仍然算数**：版式、焦点/交互规则、尺寸与动效参数、按键映射、页面结构等**设计决策**。
> - **已作废**：文中所有 Flutter 实现细节 —— `flutter build`、`StatelessWidget`/`Focus`/
>   `FocusTraversalPolicy`、Riverpod、GoRouter、media_kit、`lib/**` 路径、`.dart` 文件名。
>   它们描述的代码**不存在了**，看实现请直接读 `ui/tv/`。
>
> 本文档记录 LinPlayer TV 端（Android TV 和 Android 平板）的完整设计决策。
>
> 状态：设计定稿；UI 已按本文重建于新栈
> 关联文档：./DESIGN.md（手机端与桌面端设计）、./NATIVE_RENDERING.md（桌面视频合成）

---

## 目录

- [一、概述](#一概述)
- [二、架构决策](#二架构决策)
- [三、构建策略](#三构建策略)
- [四、导航架构](#四导航架构)
- [五、焦点系统](#五焦点系统)
- [六、首页设计](#六首页设计)
- [七、播放页设计](#七播放页设计)
- [八、详情页设计](#八详情页设计)
- [九、媒体库设计](#九媒体库设计)
- [十、搜索页设计](#十搜索页设计)
- [十一、服务器页设计](#十一服务器页设计)
- [十二、设置页设计](#十二设置页设计)
- [十三、添加服务器页设计](#十三添加服务器页设计)
- [十四、弹出面板设计](#十四弹出面板设计)
- [十五、视觉设计 Tokens](#十五视觉设计-tokens)
- [十六、动效规范](#十六动效规范)
- [十七、按键映射](#十七按键映射)
- [十八、播放器内核策略](#十八播放器内核策略)
- [十九、屏幕适配](#十九屏幕适配)
- [二十、状态管理架构](#二十状态管理架构)
- [二十一、日志系统](#二十一日志系统)
- [二十二、引导页设计](#二十二引导页设计)
- [二十三、遗漏与待完善项](#二十三遗漏与待完善项)

---

## 一、概述

### 1.1 定位

TV 端是 LinPlayer 的独立平台版本，覆盖：
- **Android TV**（智能电视、电视盒子、投影仪）
- **Android 平板**（触摸回退操作）


### 1.2 核心原则

- **遥控器驱动**：所有交互以 D-pad + 确认键为默认输入方式
- **焦点放大**：远距离观看需要物理尺寸变化的视觉反馈
- **单交互色**：仅使用品牌色 `#5B8DEF` 作为焦点/交互指示（参考 Apple 单 Action Blue 原则）
- **UI 后退**：封面/视频优先，UI 元素极简隐入背景
- **无阴影**：所有 UI 元素无阴影，焦点用 outline + 光晕（参考 Apple 无阴影原则）
- **强制深色**：TV 端始终深色模式，无视系统设置

### 1.3 项目架构

TV 端采用 Rust（后端核心） + React/TypeScript（前端 UI） + Tauri（跨平台壳）的现代栈实现。

- **后端核心**：`crates/` 目录
  - `crates/core/src/` — API 层、服务、共享逻辑
  - `crates/mpv/src/` — libmpv 播放器封装
  
- **前端 UI**：React/TypeScript 实现 TV 端用户界面
  - 焦点管理、导航栏、首页、播放页、详情页、媒体库、搜索、服务器、设置等页面
  
- **平台壳**：`apps/` 目录
  - `apps/android/` — Android TV 应用壳

---

## 二、架构决策

### 2.1 平台范围

| 平台 | 优先级 | 输入方式 | 构建产物 |
|------|--------|----------|----------|
| Android TV | P0 | 遥控器 + 平板触摸 | `linplayer-tv.apk` |
| Android 平板 | P0 | 触摸回退 | 复用 TV APK |

### 2.2 代码复用策略

- **复用 `core/`**：API 层、播放器服务、共享 Provider 完全复用
- **新增 `tv/`**：TV 专用 UI、专用 Provider（焦点管理、导航状态）
- **拆分平台特定 Provider**：
  - 手机端：手势状态、PiP 状态、触摸相关
  - TV 端：焦点状态、遥控器输入、TV 导航状态
  - 共享：服务器列表、播放进度、观看记录、媒体库数据

### 2.3 技术栈

| 技术 | 用途 |
|------|------|
| Flutter Focus | 焦点导航基础设施 |
| FocusTraversalPolicy | 自定义方向导航 |
| media_kit | MPV 播放器（Android TV） |
| ExoPlayer | Android TV 备选播放器 |
| Riverpod | 状态管理（复用） |
| GoRouter | TV 端路由 |

---

## 三、构建策略

### 3.1 Build Flavor

```bash
# TV 构建
flutter build apk --flavor tv

# 手机端构建
flutter build apk --flavor mobile
```

### 3.2 编译时标志

```dart
const bool isTV = bool.fromEnvironment('IS_TV_MODE', defaultValue: false);
```

### 3.3 产物命名

| 产物 | 文件名 |
|------|--------|
| TV APK | `linplayer-tv.apk` |
| 手机 APK | `linplayer-mobile.apk` |
| 桌面 ZIP | `linplayer-desktop.zip`（现有） |

---

## 四、导航架构

### 4.1 布局

```
┌─────────────────────────────────────────────┐
│  ┌────────┐ ┌─────────────────────────────┐ │
│  │        │ │                             │ │
│  │ 导航栏  │ │      右侧内容区              │ │
│  │ 240dp  │ │      (剩余宽度)              │ │
│  │        │ │                             │ │
│  │ [图标]  │ │                             │ │
│  │  首页   │ │                             │ │
│  │        │ │                             │ │
│  │ [图标]  │ │                             │ │
│  │  搜索   │ │                             │ │
│  │        │ │                             │ │
│  │ [图标]  │ │                             │ │
│  │ 服务器  │ │                             │ │
│  │        │ │                             │ │
│  │ [图标]  │ │                             │ │
│  │  设置   │ │                             │ │
│  │        │ │                             │ │
│  └────────┘ └─────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 4.2 导航栏参数

| 参数 | 值 |
|------|-----|
| 宽度 | 240dp（固定） |
| 背景色 | `#000000` |
| 导航项排列 | 图标 + 文字，纵向 |
| 导航项大小 | 56dp 高 |
| 图标尺寸 | 32dp |
| 标题字号 | 18sp |
| 未选中文字 | `#666666` |
| 选中文字 | `#FFFFFF` |
| 选中态 | 背景 `#FFFFFF20` + 左侧 4dp 品牌色竖线 + 放大 1.05x |
| 焦点顺序 | 默认在内容区；按"左"键进入导航栏；按"右"键返回内容区 |

### 4.3 导航项

| 项 | 图标 | 页面 |
|----|------|------|
| 首页 | home | 首页 Dashboard |
| 搜索 | search | 搜索页 |
| 服务器 | server | 服务器列表 |
| 设置 | settings | 设置页 |

---

## 五、焦点系统

### 5.1 核心策略

基于 Flutter 原生 `Focus` + 自定义 `FocusTraversalPolicy`，不另起焦点管理器。

### 5.2 关键组件

#### `TVFocusable`（通用焦点包裹）

```dart
class TVFocusable extends StatelessWidget {
  final Widget child;
  final VoidCallback? onSelect;
  final bool autoFocus;
  
  // 焦点态：放大 1.15x + 白色边框 + 品牌色光晕
  // 非焦点态：透明度 0.8
  // 动画：250ms，Curves.easeInOut
}
```

#### `TVFocusRow`（横向焦点行）

```dart
class TVFocusRow extends StatelessWidget {
  final List<Widget> children;
  final double spacing;
  
  // 行内焦点移动：左右键横向移动
  // 行内滚动：焦点项自动滚动到可见区域
  // 跨行对齐：按上下键时，新行自动对齐到最接近的列
}
```

#### `TVFocusGrid`（焦点网格）

```dart
class TVFocusGrid extends StatelessWidget {
  final int crossAxisCount;
  final List<Widget> children;
  
  // 网格内焦点移动：上下左右键
  // 到边缘自动加载下一页
}
```

### 5.3 焦点记忆

- 导航栏 ↔ 内容区：离开内容区时保存当前焦点索引，返回时恢复
- 页面切换：返回上一页时，恢复离开时的焦点位置
- 实现：使用 `FocusNode` 的 `requestFocus()` + 手动索引栈

### 5.4 焦点导航规则

| 场景 | 行为 |
|------|------|
| 首页内容区按"上" | 焦点移到 Hero Banner 的"播放"按钮 |
| 首页内容区按"左" | 焦点移到导航栏当前项 |
| 导航栏按"右" | 焦点返回内容区（恢复离开时的位置） |
| 播放页控制层按"上" | 焦点从底部控制栏移到进度条 |
| 播放页控制层按"下" | 焦点从进度条移到底部控制栏 |
| 列表按"上"到顶部 | 焦点移到筛选栏/导航栏 |
| 列表按"下"到底部 | 触发加载下一页（或显示"已加载全部"） |

---

## 六、首页设计

### 6.1 布局

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 内容区                             │
│         │ ┌───────────────────────────────┐  │
│         │ │ Hero Banner (占内容区 45%)     │  │
│         │ │  ┌─────────────────────────┐  │  │
│         │ │  │                         │  │  │
│         │ │  │    随机推荐/继续观看封面  │  │  │
│         │ │  │  (16:9, 全宽, 圆角 16)  │  │  │
│         │ │  │                         │  │  │
│         │ │  │  标题 24sp               │  │  │
│         │ │  │  ★ 9.1 标签1 标签2      │  │  │
│         │ │  │  [▶ 播放] [收藏] [更多]  │  │  │
│         │ │  └─────────────────────────┘  │  │
│         │ │                               │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 继续观看              [查看更多]│  │
│         │ │ ┌────┐ ┌────┐ ┌────┐        │  │
│         │ │ │封面│ │封面│ │封面│ ...     │  │
│         │ │ │进度│ │ 新 │ │ 50%│        │  │
│         │ │ └────┘ └────┘ └────┘        │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 媒体库                        │  │
│         │ │ ┌────┐ ┌────┐ ┌────┐        │  │
│         │ │ │封面│ │封面│ │封面│ ...     │  │
│         │ │ │名称│ │名称│ │名称│        │  │
│         │ │ └────┘ └────┘ └────┘        │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 电影（媒体库名）               │  │
│         │ │ ┌────┐ ┌────┐ ┌────┐ ┌────┐│  │
│         │ │ │封面│ │封面│ │封面│ │封面││  │
│         │ │ │名称│ │名称│ │名称│ │名称││  │
│         │ │ └────┘ └────┘ └────┘ └────┘│  │
│         │ ├───────────────────────────────┤  │
│         │ │ 剧集（媒体库名）               │  │
│         │ │ ...                           │  │
│         │ └───────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 6.2 参数

| 区域 | 参数 | 值 |
|------|------|-----|
| 内容区 | 内边距 | 左 32dp，右 32dp，上 24dp，下 24dp |
| Hero Banner | 高度 | 内容区 45% |
| Hero Banner | 圆角 | 16dp |
| Hero Banner | 渐变遮罩 | 底部 40% 从透明到深色 |
| 信息区 | 位置 | 左下角，距左 24dp，距下 24dp |
| 标题 | 字号 | 24sp |
| 评分 | 字号 | 16sp，品牌色 + 星星图标 |
| 标签 | 字号 | 14sp，最多 5 个，背景 `#FFFFFF20` + 圆角 8dp |
| 播放按钮 | 尺寸 | 宽 240dp，高 56dp，圆角 12dp，品牌色背景 |
| 内容行 | 行间距 | 48dp |
| 行标题 | 字号 | 20sp 粗体 |
| 查看更多 | 字号 | 16sp，品牌色 |
| 海报 | 比例 | 16:9 |
| 海报 | 宽度 | 280dp（高度 157dp） |
| 海报间距 | 行内间距 | 16dp |
| 焦点态 | 放大 | 1.15x |
| 焦点态 | 边框 | 3px 白色 |
| 焦点态 | 光晕 | 20px blur，品牌色 30% 透明度 |
| 非焦点态 | 透明度 | 0.8 |

### 6.3 Hero 内容策略

- **优先**：继续观看（第一个条目，如果正在观看或有新集）
- **降级**：无继续观看时显示随机推荐（8 个，固定从第一个开始）
- **下方行**：继续观看（剩余条目，不含 Hero 已展示的第一个）/ 媒体库 / 各媒体库最近更新
- **新集**：有更新时排到第一个展示
- **已看完当前集**：如果已看完当前集，自动将下一集（未看）加入"继续观看"
- **轮播**：
  - 自动轮播：10 秒间隔，淡入淡出 500ms，`Curves.easeInOut`
  - 手动切换：遥控器左右键，焦点在 Hero 时切换
  - 暂停轮播：焦点离开 Hero 区域时暂停自动轮播
  - 触摸模式：左右滑动切换（平板）
  - 指示器：底部圆点，当前项品牌色实心，非当前项 `#FFFFFF40`

### 6.4 继续观看行策略

- **包含内容**：
  - 有进度未看完（进度 1%-99%）
  - 已看完当前集，下一集未看（自动加入继续观看）
- **不包含**：
  - 整季看完且无新集（暂时隐藏）
- **排序**：
  - 有新集更新 → 排到第一个
  - 有进度未看完 → 按最近观看时间排序
  - 已看完当前集，下一集未看 → 按看完时间排序
- **卡片展示**：
  - 封面 + 标题 + 进度条（底部品牌色）+ 集数标签
  - 新集：右上角"新"标签（红色/品牌色）
  - 已看完但下一集未看：显示"S1E6 · 下一集"标签

---

## 七、播放页设计

### 7.1 双模式架构

| 模式 | 触发 | 视觉 |
|------|------|------|
| 遥控器模式 | 检测到遥控器按键 | 显示焦点边框 + 焦点放大 |
| 触摸模式 | 检测到触摸输入 | 隐藏焦点边框，直接点击触发 |

### 7.2 控制层显隐

- 遥控器按键 / 触摸屏幕 → 控制层淡入（300ms）
- 5 秒无操作 / 触摸空白区域 / 按返回键（控制层已显示时）→ 控制层淡出（400ms）

### 7.3 控制层布局

```
┌─────────────────────────────────────────────┐
│ [←] 标题(滚动)  [跳过] [···]             │  ← 顶部栏（透明）
│                                             │
│                                             │
│                                             │
│                                             │
│          [进度条 + 时间]                    │  ← 进度条区域
│                                             │
│ [上集] [快退] [▶/⏸] [快进] [下集] [字] [音] [更]│  ← 底部控制栏
└─────────────────────────────────────────────┘
```

### 7.4 顶部栏

| 位置 | 元素 | 说明 |
|------|------|------|
| 左侧 | 返回按钮 | 退出播放页 |
| 中间 | 集标题 | 太长则滚动显示 |
| 右侧 | 跳过按钮 | 片头/片尾跳过设置（如果有章节数据） |
| 右侧 | 更多按钮 | 呼出右侧滑入面板（字幕/音轨/倍速/画面比例/内核切换/章节等） |

### 7.5 进度条区域

- 距底部：120dp（给底部控制栏留空间）
- 左右内边距：48dp
- 进度条：高度 4dp（焦点时 8dp），已播放部分品牌色
- 时间：左侧当前时间，右侧剩余时间（点击切换）
- 遥控器焦点在进度条时：左右键快退/快进（固定步进）
- 触摸：直接拖拽进度条

### 7.6 底部控制栏

| 位置 | 按钮 | 功能 |
|------|------|------|
| 左侧 | 上一集 | 切换集 |
| 左侧 | 快退 | 10s（默认） |
| 中央 | 播放/暂停 | 播放/暂停 |
| 右侧 | 快进 | 10s（默认） |
| 右侧 | 下一集 | 切换集 |
| 右侧 | 字幕 | 呼出字幕设置面板 |
| 右侧 | 音轨 | 呼出音轨设置面板 |
| 右侧 | 更多 | 呼出更多面板 |

### 7.7 快进/快退策略

| 输入 | 步进 | 说明 |
|------|------|------|
| 短按左/右 | 10s | 默认步进，可在设置调整 |
| 长按左/右 | 加速连续步进 | 速度逐渐加速（10s → 30s → 1min → 5min） |
| 进度条焦点 + 左/右 | 10s | 精确步进 |

### 7.8 触摸手势（平板模式）

保留手机端全部手势：

| 手势 | 功能 |
|------|------|
| 双击左侧 | 快退（可在设置自定义） |
| 双击中间 | 播放/暂停（可在设置自定义） |
| 双击右侧 | 快进（可在设置自定义） |
| 长按屏幕 | 临时快进（倍速可在设置自定义） |
| 左侧上下滑 | 调节亮度 |
| 右侧上下滑 | 调节音量 |
| 左右滑 | 调节进度 |

#### 播放页控制层焦点导航

- 默认焦点：播放/暂停按钮（底部控制栏中央）
- 按"上"键：焦点从底部控制栏 → 进度条
- 按"下"键（焦点在进度条）：回到底部控制栏
- 按"左/右"键（焦点在进度条）：快退/快进（固定步进）
- 长按左右键（焦点在进度条）：连续步进（加速）
- 按"上"键（焦点在进度条）：焦点到顶部栏（返回/跳过/更多）
- 按"下"键（焦点在顶部栏）：回到进度条
- 按"返回"键（控制层已显示）：隐藏控制层
- 触摸空白区域：隐藏控制层
- 无操作 5 秒：自动隐藏控制层（400ms 淡出）
- 长按进度条：直接拖拽到目标位置（触摸模式）或连续步进（遥控器模式）

### 7.9 更多面板（右侧滑入）

| 分组 | 项 |
|------|-----|
| 播放设置 | 倍速、画面比例、内核切换 |
| 轨道设置 | 字幕、音轨、次字幕 |
| 其他 | 投屏、统计信息、定时关闭、截图、锁定 |

### 7.10 暂停状态

- 暂停时：视频画面定格，控制层显示
- 屏幕中央显示半透明圆形背景 + 白色暂停图标
  - 圆形背景：直径 80dp，品牌色 `#5B8DEF` 40% 透明度
  - 暂停图标：白色，40dp
  - 动画：淡入 200ms
  - 恢复播放：图标淡出 200ms
- 控制层中的播放按钮显示为"▶"（三角形）
- 触摸空白区域或按确认键：恢复播放

### 7.11 章节跳转面板

- 章节入口在"更多"面板中（如果有章节数据）
- 章节面板：右侧滑入，列出所有章节
  - 每项：章节名 + 时间戳
  - 焦点选中跳转
- 进度条显示章节标记：小竖线，品牌色，悬浮提示章节名
- 无章节数据时，章节入口隐藏
- 跳过片头/片尾：章节面板顶部显示"片头"和"片尾"快速跳转（如果有）

### 7.12 播放完毕处理

- 播放完毕（进度 100%）→ 自动上报 Emby 播放停止
- 自动播放下一集：默认开启（设置可关闭）
- 自动播放时：淡入下一集（300ms），不显示播放页控制层
- 无下一集时：停在结尾画面，显示控制层（方便用户操作）
- 播放页控制层显示"播放完毕"提示（3 秒，Toast）
- 触摸模式：点击"下一集"按钮或滑动选择其他集

---

## 八、详情页设计

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 内容区                             │
│         │ ┌───────────────────────────────┐  │
│         │ │ Hero 区域 (占内容区 40%)       │  │
│         │ │  封面 16:9，左下角信息           │  │
│         │ │  标题 + 评分 + 标签 + 简介        │  │
│         │ │  [▶ 播放] [收藏] [更多]          │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 继续观看（横滑）                │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 季度选择（横滑，2:3 海报）       │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 集数选择（横滑，16:9）          │  │
│         │ ├───────────────────────────────┤  │
│         │ │ 演职人员（横滑，头像圆形）       │  │
│         │ └───────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 8.2 集详情页

- 顶部：集封面/截图（16:9，占 35%）
- 左下角：集标题 + 时长 + 文件大小 + 简介
- 按钮：播放（主按钮）+ 收藏 + 标记已看/未看
- 下方：版本信息（视频编码、音频、字幕）+ 上一集/下一集快速切换
- 返回：回到剧集详情页
- 焦点默认在"播放"按钮

### 8.3 电影详情页

- 结构同剧详情页 Hero 区域
- 播放按钮下方：版本信息行（视频编码 + 分辨率 + 音频轨道 + 字幕轨道）
- 右侧滑入面板：线路/版本/音频/字幕选择

### 8.4 收藏按钮

- 位置：Hero 区域下方按钮组（播放按钮旁边）
- 状态：
  - 未收藏：空心星星图标，灰色
  - 已收藏：实心星星图标，品牌色
- 焦点态：放大 1.15x + 边框 + 光晕
- 操作：确认键切换，Toast 提示"已收藏" / "已取消收藏"
- 触摸：直接点击切换

---

## 九、媒体库设计

### 9.1 媒体库详情页

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 筛选栏（横向排列）                │
│         │ [加入日期↑] [标题] [首映日期] [评级] [评分]│
│         ├─────────────────────────────────────┤
│         │ 内容网格                            │
│         │ ┌────┐ ┌────┐ ┌────┐              │
│         │ │海报│ │海报│ │海报│              │
│         │ │名称│ │名称│ │名称│              │
│         │ └────┘ └────┘ └────┘              │
│         │ ...（无限滚动）                      │
│         └─────────────────────────────────────┘
```

### 9.2 参数

| 参数 | 值 |
|------|-----|
| 筛选栏高度 | 56dp |
| 筛选项 | 加入日期、标题、首映日期、官方评级、评分 |
| 排序指示 | 每项右侧箭头（↑ 升序 / ↓ 降序），默认升序 |
| 海报比例 | 2:3 |
| 网格列数 | 2 列（1080p）/ 3 列（4K/平板） |
| 海报宽度 | 200dp（1080p）/ 160dp（4K 3列） |
| 网格间距 | 24dp |
| 焦点态 | 放大 1.15x + 边框 + 光晕 |
| 无限滚动 | 到底部触发加载 |

---

## 十、搜索页设计

### 10.1 布局

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 搜索页                             │
│         │ ┌──────────┐ ┌─────────────────┐  │
│         │ │ 搜索栏    │ │ 虚拟键盘        │  │
│         │ │ 当前输入   │ │  Q W E R T Y U  │  │
│         │ ├──────────┤ │  I O P A S D F  │  │
│         │ │ 搜索历史  │ │  G H J K L Z X  │  │
│         │ │ · 历史1  │ │  C V B N M 空格  │  │
│         │ │ · 历史2  │ │  123 退格 确认   │  │
│         │ ├──────────┤ │                  │  │
│         │ │ 热门标签  │ │                  │  │
│         │ │ [标签1]  │ └─────────────────┘  │
│         │ │ [标签2]  │                      │
│         │ └──────────┘                      │
└─────────────────────────────────────────────┘
```

### 10.2 参数

| 参数 | 值 |
|------|-----|
| 搜索栏 | 高度 56dp，圆角 pill，品牌色焦点边框 |
| 左侧区域 | 占内容区 40% |
| 键盘区域 | 占内容区 60% |
| 键盘按键 | 48dp 方格，焦点放大 1.15x + 品牌色高亮 |
| 键盘布局 | QWERTY 26 键 + 数字/符号切换 + 空格 + 退格 + 确认 |
| 搜索历史 | 最多 5 条，点击直接搜索 |
| 热门标签 | 焦点选中直接搜索 |
| 确认键 | 呼出键盘 |
| 触摸 | 点击输入框 → 系统键盘（平板） |

### 10.3 聚合搜索

- 开启聚合开关后，向所有已连接服务器发搜索请求
- 结果按服务器分组展示
- 聚合排序优先级：服务器名称 / 资源大小 / 集数 / 资源质量

### 10.4 搜索历史

- 搜索历史与手机端共享（通过 `shared_preferences` 的相同 key）
- TV 端搜索历史最多保存 20 条
- 搜索历史在 TV 端和手机端同步显示
- 清除历史：在搜索页按菜单键/更多键呼出"清除历史"选项

---

## 十一、服务器页设计

### 11.1 布局

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 服务器页                           │
│         │ ┌────┐ ┌────┐                  │
│         │ │图标│ │图标│                  │
│         │ │名称│ │名称│                  │
│         │ │备注│ │备注│                  │
│         │ └────┘ └────┘                  │
│         │ ...                            │
│         │ [+ 添加服务器]                  │
└─────────────────────────────────────────────┘
```

### 11.2 参数

| 参数 | 值 |
|------|-----|
| 列数 | 2 列（1080p）/ 3 列（4K/平板） |
| 卡片间距 | 24dp |
| 服务器图标 | 80dp，圆角 16dp |
| 名称 | 20sp，居中，最大 1 行 |
| 备注 | 14sp，居中，灰色，最大 1 行 |
| 焦点态 | 放大 1.15x + 边框 + 光晕 |
| 操作面板 | 右侧面板：编辑信息/重新登录/服务器线路/修改图标/修改备注/删除 |
| 排序 | 长按确认键进入排序模式，卡片虚线边框，上下左右移动焦点，确认键放置 |
| 空状态 | 中央显示"暂无服务器" + 大按钮"添加服务器" |

---

## 十二、设置页设计

### 12.1 双栏 Master-Detail 布局

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 左栏(40%)      │ 右栏(60%)       │
│         │ 通用设置        │ 外观            │
│         │ 播放器设置       │ 深色/浅色/跟随   │
│         │ 弹幕设置         │ 语言            │
│         │ 关于            │ 启动页           │
│         │ 备份恢复        │ 缓存管理         │
│         │                │ 聚合搜索优先级    │
│         │                │ ...             │
└─────────────────────────────────────────────┘
```

### 12.2 参数

| 参数 | 值 |
|------|-----|
| 左栏宽度 | 40%，最大 400dp |
| 右栏宽度 | 60% |
| 两栏间距 | 32dp |
| 分类项 | 56dp，图标 24dp + 文字 18sp |
| 设置项 | 56dp（开关/下拉）或 72dp（带描述） |
| 焦点切换 | 默认在分类列表；按"右"进入右栏；按"左"返回左栏 |
| 切换分类 | 右栏内容淡入切换（250ms） |
| 复杂设置 | 右栏内分组（"轨道"/"样式"/"同步"），不嵌套子面板 |

### 12.3 设置项

#### 通用设置

| 设置项 | 选项 |
|--------|------|
| 外观 | 深色（强制）/ 浅色（强制）/ 跟随系统 |
| 语言 | 跟随系统 / 中文 / English |
| 启动页 | 首页 / 上次浏览位置 |
| 缓存管理 | 显示缓存大小 + 清除缓存按钮 |
| 聚合搜索优先级 | 服务器名称 / 资源大小 / 集数 / 资源质量 |

#### 播放器设置（TV 端专属）

| 设置项 | 选项 |
|--------|------|
| 播放器内核 | 列出当前平台所有可用内核，默认 mpv（Android TV 可选 ExoPlayer） |
| 默认播放速度 | 0.25x ~ 4x |
| 快进步长 | 5s / 10s / 15s / 30s（默认 10s） |
| 长按快进倍速 | 2x / 3x / 4x |
| 双击交互 | 左/中/右行为各自配置（TV 触摸模式生效） |
| 硬件解码 | 开关 |
| 自动播放下一集 | 开关 |
| 休眠定时器默认值 | 15min / 30min / 60min / 关闭 |
| 后台播放 | TV 端不支持，设置中隐藏 |
| 画中画 | TV 端不支持，设置中隐藏 |

#### 弹幕设置（TV 端专属）

| 设置项 | 选项 | 默认值 |
|--------|------|--------|
| 弹幕开关 | 全局开关 | 关闭 |
| 透明度 | 滑块 0% ~ 100% | 70% |
| 字号 | 滑块 | 1.5x（24sp 基准） |
| 速度 | 慢 / 正常 / 快 | 慢 |
| 密度 | 低 / 中 / 高 | 低 |
| 防遮挡区域 | 上 20% / 下 20% / 上下 10% / 无 | 上 20% |
| 屏蔽词管理 | 编辑屏蔽词列表 | 复用手机端 |

#### 屏幕旋转（横竖屏）

- Android TV：固定横屏（Landscape），不处理旋转
- Android 平板（使用 TV APK）：强制横屏模式，不响应设备旋转
- 播放页：视频按原始比例显示，不支持手动旋转（TV 上不需要）

---

## 十三、添加服务器页设计

### 13.1 布局

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 添加服务器                        │
│         │ 服务器地址 [https://...]          │
│         │ 路径     [/emby]                 │
│         │ 用户名   [________]              │
│         │ 密码     [________] [显示]        │
│         │                                  │
│         │ [  保存并登录  ]                  │
│         │                                  │
│         │ ─── 或 ───                        │
│         │ [  手机扫码添加  ]                 │
│         │ [  批量解析  ] [  导入配置  ]       │
│         │                                  │
│         │ 虚拟键盘（呼出时覆盖底部）          │
└─────────────────────────────────────────────┘
```

### 13.2 输入方式

| 方式 | 说明 |
|------|------|
| 虚拟键盘 | 26 键 QWERTY，呼出时覆盖底部 320dp，表单自动上移 |
| 外接键盘 | 蓝牙/USB 键盘直接输入（TV 常见） |
| 手机扫码 | TV 显示二维码 → 手机扫描输入 → 推送服务器信息到 TV |
| 批量解析 | 粘贴分享文本 → 解析多线路 |
| 导入配置 | 从 JSON 文件导入 |

---

## 十四、弹出面板设计

### 14.1 统一组件：`TVSidePanel`

```
┌─────────────────────────────────────────────┐
│                              ┌────────────┐ │
│  背景模糊 + 深色遮罩          │ 右侧面板    │ │
│  (#000000AA)                 │ 400dp 宽    │ │
│                              │ 全高        │ │
│                              │ 左侧圆角 16  │ │
│                              │            │ │
│                              │ 标题区      │ │
│                              │ ────────── │ │
│                              │ 列表项      │ │
│                              │ 列表项      │ │
│                              │ 列表项      │ │
│                              │            │ │
│                              └────────────┘ │
└─────────────────────────────────────────────┘
```

### 14.2 参数

| 参数 | 值 |
|------|-----|
| 宽度 | 400dp 固定 |
| 高度 | 全屏 |
| 圆角 | 右侧无，左侧 16dp |
| 背景 | 模糊 + 深色遮罩 `#000000AA` |
| 标题区 | 高 80dp，标题 24sp 粗体，右上角关闭按钮 |
| 内容区 | 列表项 56dp（单行）或 72dp（带描述） |
| 分组标题 | 14sp 大写灰色，上下间距 16dp |
| 焦点项 | 背景 `#FFFFFF20` + 左侧 4dp 品牌色竖线 |
| 非焦点项 | 背景透明，文字 `#FFFFFFCC` |
| 动画 | 入场：从右滑入 300ms；退场：向右滑出 250ms |
| 关闭 | 返回键 / 点击背景遮罩（触摸） |

### 14.3 使用场景

| 场景 | 面板内容 |
|------|----------|
| 播放页"更多" | 倍速、画面比例、内核切换、投屏、统计、定时、截图、锁定 |
| 字幕设置 | 轨道列表 + 样式设置 + 同步调节 |
| 音轨设置 | 轨道列表 + 同步调节 |
| 选集 | 季度选择 + 集数列表（条形/矩形） |
| 服务器操作 | 编辑/重登/线路/图标/备注/删除 |
| 电影版本 | 线路/版本/音频/字幕选择 |

---

## 十五、视觉设计 Tokens

### 15.1 颜色

| Token | 值 | 用途 |
|-------|-----|------|
| `brand` | `#5B8DEF` | 单交互色（焦点、按钮、进度条、标签） |
| `brand-focus` | `#6B9DFF` | 焦点态稍亮 |
| `bg-nav` | `#000000` | 导航栏背景（纯黑，隐入电视边框） |
| `bg-content` | `#121212` | 内容区背景（近黑） |
| `bg-panel` | `#1A1A1A` | 右侧面板背景（微亮于内容区） |
| `bg-focus` | `#FFFFFF20` | 焦点项背景（白色 20% 透明度） |
| `text-primary` | `#FFFFFF` | 主文字 |
| `text-secondary` | `#AAAAAA` | 副文字（浅灰，比苹果稍暗） |
| `text-muted` | `#666666` |  muted 文字 |
| `border-focus` | `#FFFFFF` | 焦点边框 |
| `glow-focus` | `#5B8DEF4D` | 焦点光晕（品牌色 30%） |
| `divider` | `#333333` | 分割线 |
| `overlay` | `#000000AA` | 遮罩层（黑色 67%） |
| `progress-played` | `#5B8DEF` | 进度条已播放 |
| `progress-unplayed` | `#333333` | 进度条未播放 |

### 15.2 字体

| Token | 值 | 用途 |
|-------|-----|------|
| `font-family` | 系统字体（Roboto / 默认） | 全平台 |
| `hero-title` | 24sp / 600 | Hero 标题 |
| `section-title` | 20sp / 600 | 行标题 |
| `body` | 24sp / 400 | TV 正文（1.4x 手机端） |
| `caption` | 16sp / 400 | 说明文字 |
| `tag` | 14sp / 400 | 标签 |
| `button` | 18sp / 600 | 按钮文字 |
| `nav` | 18sp / 400 | 导航栏文字 |
| `time` | 16sp / 400 | 时间显示 |

### 15.3 间距

| Token | 值 | 用途 |
|-------|-----|------|
| `spacing-xs` | 8dp | 最小间距 |
| `spacing-sm` | 16dp | 小间距 |
| `spacing-md` | 24dp | 中间距 |
| `spacing-lg` | 32dp | 大间距 |
| `spacing-xl` | 48dp | 超大间距 |
| `spacing-section` | 64dp | 区块间距 |

### 15.4 圆角

| Token | 值 | 用途 |
|-------|-----|------|
| `radius-none` | 0dp | Hero 全宽、全屏 |
| `radius-sm` | 8dp | 卡片、海报 |
| `radius-md` | 12dp | 按钮 |
| `radius-lg` | 16dp | Hero Banner、面板 |
| `radius-pill` | 9999dp | 搜索栏、CTA 按钮、标签 |
| `radius-full` | 50% | 圆形头像、图标按钮 |

### 15.5 焦点指示

| 参数 | 值 |
|------|-----|
| 放大 | 1.15x |
| 边框 | 3px 白色实线 |
| 光晕 | 15px 模糊，品牌色 30% 透明度 |
| 动画时长 | 250ms |
| 动画曲线 | `Curves.easeInOut` |
| 非焦点透明度 | 0.8 |
| 按压缩放 | 0.95x（参考 Apple） |

---

## 十六、动效规范

### 16.1 页面切换

| 动画 | 参数 |
|------|------|
| 类型 | 淡入 + 轻微缩放 |
| 入场 | `opacity: 0 → 1`, `scale: 0.95 → 1.0` |
| 时长 | 300ms |
| 曲线 | `Curves.easeOut` |
| 返回 | 反向动画 |

### 16.2 焦点移动

| 动画 | 参数 |
|------|------|
| 放大 | 1.15x，250ms，`Curves.easeInOut` |
| 边框/光晕 | 与放大同步，250ms |
| 透明度 | 非焦点 0.8 → 焦点 1.0，250ms |
| 相邻项推开 | 间距自动扩展，250ms |

### 16.3 行内滚动

| 动画 | 参数 |
|------|------|
| 滚动 | 300ms，`Curves.easeInOut` |
| 目标 | 焦点项完整露出 |

### 16.4 行间跳转

| 动画 | 参数 |
|------|------|
| 整行滚动 | 400ms，`Curves.easeInOut` |
| 目标 | 新行完全可见 |
| 列对齐 | 新行自动对齐到最接近的列 |

### 16.5 面板动画

| 动画 | 参数 |
|------|------|
| 入场 | 从右滑入 300ms，`Curves.easeInOut` |
| 背景遮罩 | 淡入 250ms，`Curves.easeOut` |
| 退场 | 向右滑出 250ms |
| 背景遮罩淡出 | 250ms |

### 16.6 控制层显隐

| 动画 | 参数 |
|------|------|
| 显示 | 淡入 300ms |
| 隐藏 | 淡出 400ms（无操作时） |
| 即时隐藏 | 0ms（触摸空白/按返回） |

---

## 十七、按键映射

### 17.1 Android TV 遥控器

| 按键 | 全局行为 | 播放页（控制层隐藏） | 播放页（控制层显示） | 页面内 |
|------|---------|---------------------|---------------------|--------|
| **方向键 上** | 焦点向上移动 | 呼出控制层 | 焦点向上（底部→进度条） | 列表/网格向上 |
| **方向键 下** | 焦点向下移动 | 呼出控制层 | 焦点向下（进度条→底部） | 列表/网格向下 |
| **方向键 左** | 焦点向左 | 呼出控制层 | 焦点向左；进度条焦点时快退 | 焦点向左/返回 |
| **方向键 右** | 焦点向右 | 呼出控制层 | 焦点向右；进度条焦点时快进 | 焦点向右/前进 |
| **确认键** | 触发选中项 | 呼出控制层 | 触发焦点项 | 触发选中项 |
| **返回键** | 返回上一页 | 退出播放 | 隐藏控制层 | 返回上一页 |
| **菜单键** | 呼出更多面板 | 呼出"更多"面板 | 呼出"更多"面板 | 呼出当前项面板 |
| **主页键** | 系统行为 | 画中画（不支持） | 系统行为 | 系统行为 |
| **音量 +/-** | 系统音量 | 系统音量 | 系统音量 | 系统音量 |
| **播放/暂停** | 全局播放/暂停 | 播放/暂停 | 播放/暂停 | 播放/暂停 |

### 17.2 长按行为

| 场景 | 长按确认键行为 |
|------|---------------|
| 服务器页 | 进入排序模式（卡片虚线边框，焦点移动，确认放置） |
| 播放页 | 长按倍速（临时快进，松开后恢复） |
| 进度条 | 长按左右键连续步进（加速） |

### 17.3 触摸映射（平板）

| 手势 | 行为 |
|------|------|
| 点击 | 直接触发，无焦点边框 |
| 双击左 | 快退（可在设置自定义） |
| 双击中 | 播放/暂停（可在设置自定义） |
| 双击右 | 快进（可在设置自定义） |
| 长按 | 临时快进（倍速可在设置自定义） |
| 左上下滑 | 调节亮度 |
| 右上下滑 | 调节音量 |
| 左右滑 | 调节进度 |
| 点击空白 | 控制层显隐 |

---

## 十八、播放器内核策略

### 18.1 Android TV

| 内核 | 默认 | 说明 |
|------|------|------|
| MPV | 默认 | 全格式、HDR、Dolby Vision、PGS/SUP 字幕、Anime4K |
| ExoPlayer | 可选 | 硬件解码优化，设置中切换 |

### 18.2 设置中内核切换

- 列出当前平台所有可用内核
- 不可用内核禁用并标注原因
- 默认 MPV
- 切换后下次播放生效

---

## 十九、屏幕适配

### 19.1 基准

- 基准屏幕：1920x1080（1080p TV）
- 基准密度：1.0x（TV 通常 1.0x 或 1.5x）

### 19.2 动态列数

```dart
int calculateColumns(double screenWidth) {
  if (screenWidth < 1200) return 2;
  if (screenWidth < 1800) return 3;
  return 4;
}
```

### 19.3 固定尺寸

| 元素 | 尺寸 | 说明 |
|------|------|------|
| 导航栏宽度 | 240dp | 不随屏幕变化 |
| 内容区内边距 | 32dp | 固定 |
| 海报间距 | 24dp | 固定 |
| 行间距 | 48dp | 固定 |
| Hero 高度 | 屏幕高度 45% | 固定比例 |

### 19.4 字体缩放

- 使用 Flutter 的 `textScaleFactor` 跟随系统（TV 用户可能调整系统字体大小）
- 基准 24sp 已考虑 TV 远距离观看

---

## 二十、状态管理架构

### 20.1 共享 Provider（`core/providers/`）

| Provider | 用途 | 平台 |
|----------|------|------|
| `serverProvider` | 服务器列表、当前活跃服务器 | 全平台 |
| `playbackProvider` | 播放状态、进度、当前集 | 全平台 |
| `mediaLibraryProvider` | 媒体库、最近更新 | 全平台 |
| `watchHistoryProvider` | 观看记录、继续观看 | 全平台 |
| `authProvider` | 认证、Token | 全平台 |
| `settingsProvider` | 用户设置 | 全平台 |

### 20.2 TV 专用 Provider（`tv/providers/`）

| Provider | 用途 |
|----------|------|
| `tvFocusProvider` | 全局焦点状态（当前焦点位置、焦点历史） |
| `tvNavigationProvider` | TV 导航状态（当前页面、导航栏选中项） |
| `tvInputModeProvider` | 输入模式（遥控器/触摸） |
| `tvControlLayerProvider` | 播放页控制层显隐状态、计时器 |
| `tvKeyboardProvider` | 虚拟键盘状态（当前布局、输入内容） |
| `tvLogServerProvider` | 日志下载临时 HTTP 服务器 |

### 20.3 手机端专用 Provider（`ui/providers/`）

| Provider | 用途 |
|----------|------|
| `gestureProvider` | 手势状态（亮度/音量/进度） |
| `pipProvider` | 画中画状态 |
| `touchProvider` | 触摸相关状态 |

---

## 二十一、日志系统

### 21.1 日志存储

- TV 端日志存储在本地文件（`path_provider` 的文档目录）
- 日志文件按日期滚动，保留最近 7 天
- 日志格式：时间戳 + 级别 + 模块 + 消息
- 日志级别：DEBUG / INFO / WARNING / ERROR

### 21.2 日志下载

TV 端无法方便查看日志，通过局域网 HTTP 服务器 + 手机扫码下载。

#### 方案：内置临时 HTTP 服务器

- **设置页 → 关于 → 日志** 显示二维码
- 二维码内容：局域网下载 URL（如 `http://192.168.1.100:8080/logs`）
- TV 端启动临时 HTTP 服务器（只在日志页面存在时运行）
- 手机扫描二维码 → 浏览器打开 → 下载日志文件

#### 服务器参数

| 参数 | 值 |
|------|-----|
| 端口 | 8080（如果占用则随机） |
| 绑定 IP | 仅局域网 IP（`192.168.x.x` / `10.x.x.x`），不绑定公网 |
| 路由 `/logs` | 下载全部日志（zip 压缩） |
| 路由 `/logs/latest` | 下载最近 1 天日志 |
| 路由 `/logs/session` | 下载当前会话日志 |

#### 页面设计

```
┌─────────────────────────────────────────────┐
│  导航栏 │ 日志下载                           │
│         │                                  │
│         │   ┌────────────────────────┐     │
│         │   │                        │     │
│         │   │     [二维码大图]        │     │
│         │   │                        │     │
│         │   └────────────────────────┘     │
│         │                                  │
│         │   http://192.168.1.100:8080/logs │
│         │                                  │
│         │   [ 下载全部日志 ]              │
│         │   [ 下载最近1天 ]               │
│         │   [ 下载当前会话 ]               │
│         │                                  │
│         │   服务器运行中...               │
│         └──────────────────────────────────┘
```

| 参数 | 值 |
|------|-----|
| 二维码 | 中央大图，200x200dp |
| URL 文字 | 16sp，可复制（平板触摸） |
| 下载按钮 | 纵向排列，56dp 高，品牌色背景 |
| 状态提示 | 14sp，灰色 |
| 安全提示 | "仅局域网可用，退出页面后服务器关闭" |

#### 生命周期

- 进入日志页面 → 启动 HTTP 服务器
- 退出日志页面（返回键/导航切换） → 关闭 HTTP 服务器
- 应用后台 → 保持服务器运行（如果页面仍在）
- 应用退出 → 关闭服务器

#### 日志保留策略

| 策略 | 说明 |
|------|------|
| 保留周期 | 7 天滚动 |
| 单个文件大小 | 最大 5MB，超过则新建文件 |
| 总大小限制 | 50MB，超过则删除最旧文件 |
| 日志格式 | `[2024-06-13 10:30:45] [INFO] [Player] 开始播放` |

---

## 二十二、遗漏与待完善项

### 22.1 已确认的设计（已讨论完成）

| 项 | 状态 | 说明 |
|----|------|------|
| 加载状态设计 | ✅ 确认 | 骨架屏（静态占位） + 首次启动中央加载圆圈 |
| 错误/空状态设计 | ✅ 确认 | 全屏中央为主 + 顶部轻量提示条为辅 |
| 通知/Toast 设计 | ✅ 确认 | 中央 Toast（中央偏下，3 秒停留） |
| 弹幕 TV 策略 | ✅ 确认 | 默认关闭，支持手动开启，TV 专属参数 |
| 引导页/首次使用 | ✅ 确认 | 3 页引导页（遥控器/导航栏/焦点系统） |
| 播放失败处理 | ✅ 确认 | 自动重试 1 次 + 全屏错误 + 手动切换线路/内核 |
| 网络切换处理 | ✅ 确认 | 播放时自动缓冲恢复，超时后报错 |
| 自动更新 | ✅ 确认 | 启动时检查 + Toast 提示，支持强制更新 |
| 性能基准 | ✅ 确认 | 启动 3 秒，页面切换 400ms，焦点 60fps，内存 < 200MB |
| 日志系统 | ✅ 确认 | 本地存储 + 临时 HTTP 服务器 + 二维码下载 |

### 22.2 待编码实现时验证的项

| 项 | 优先级 | 说明 |
|----|--------|------|
| media_kit Android TV 兼容性 | P0 | 需验证 libmpv 在 Android TV 上的构建和运行 |
| Flutter Focus 在 TV 上的性能 | P0 | 大量焦点项时的帧率，是否掉帧 |
| 虚拟键盘输入效率 | P1 | 26 键在 TV 上的实际体验，是否需要 T9 备选 |
| 焦点导航流畅度 | P0 | 跨行对齐的动画流畅度，是否卡顿 |
| 章节跳转 | P2 | 有章节数据时优先按章节跳转 |

### 22.3 后续版本（V2）规划

| 项 | 优先级 | 说明 |
|----|--------|------|
| 多用户切换 | P3 | TV 端多用户登录（家庭共享） |
| 家长控制 | P3 | PIN 码、内容分级 |
| 无障碍支持 | P2 | TalkBack、VoiceOver、高对比度 |
| 语音搜索 | P2 | Android TV Assistant 集成 |
| 手机遥控 | P3 | 手机作为 TV 遥控器（局域网控制） |
| 局域网自动发现 | P2 | mDNS/SSDP 扫描 Emby 服务器 |
| 播放队列 UI | P3 | 显式播放队列（当前为隐式） |
| 直播电视（LiveTV） | P3 | Emby LiveTV 支持 |
| 插件系统 | P3 | 手机端 V2 规划，TV 端同步 |

---

## 二十三、引导页设计

### 23.1 触发条件

- 首次启动且本地无服务器数据时展示
- 设置页 → 关于 → [重新查看引导] 可手动触发

### 23.2 页面参数

| 参数 | 值 |
|------|-----|
| 页数 | 3 页 |
| 背景 | 深色 `#121212` |
| 图标 | 中央，80dp，品牌色 |
| 标题 | 24sp，白色，居中 |
| 说明 | 18sp，灰色，居中，最多 2 行 |
| 导航 | 底部圆点指示器 + 左右键翻页 + 确认键完成 |
| 跳过 | 右上角 [跳过] 按钮（始终可见，焦点选中） |
| 存储 | `shared_preferences` 标记 `hasSeenOnboarding` |

### 23.3 页面内容

| 页 | 图标 | 标题 | 说明 |
|----|------|------|------|
| 1 | 遥控器 | 使用遥控器导航 | 使用方向键移动焦点，确认键选择，返回键返回 |
| 2 | 导航栏 | 左侧导航栏 | 按左右键在导航栏和内容区之间切换 |
| 3 | 焦点放大 | 焦点指示 | 当前项高亮放大表示选中，可执行操作 |

---

## 附录

### A. 参考设计

- Apple TV App（Watch Now、TV Shows、Movies）
- Netflix TV App
- Disney+ TV App
- YouTube TV
- Emby 官方 TV App
- Kodi

### B. 文档关联

- `./DESIGN.md`：手机端与桌面端设计
- `../README.md`：项目概述与功能特性
- `../pubspec.yaml`：依赖与构建配置

---

> 本文档跟随代码演进持续更新。任何架构或设计变更需同步更新本文档。
