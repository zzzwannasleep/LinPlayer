# 契约变更记录

`SPEC.md` 里标 `【一次做对】` 的段落(§5.0 ABI 协商、§5.1–5.3 FFI 契约、
§5.10 panic 边界、§5.11 事件队列、§7.2 surface 交接)一旦在阶段 1 出口冻结,
任何改动都要在这里留一条。

## 为什么要这个文件

这两块东西改一次,三端的绑定层、测试、已发布的客户端全部要跟着动。
没有变更记录的表现是:某端按旧契约写的代码在新核心上静默出错,
而没人记得契约什么时候变的、为什么变。

## 格式

```
## YYYY-MM-DD · <一句话摘要>

- **改了什么:** 具体到函数签名 / 字段名 / 语义
- **为什么:** 触发这次变更的问题
- **兼容性:** 破坏性 / 向后兼容;旧版客户端会怎样
- **三端跟进:** Android / 桌面 / Apple 各自的 issue 号
```

---

## 待冻结

契约尚未冻结(阶段 1 未完成)。冻结前的修改直接改 `SPEC.md`,不必在此登记。

冻结条件见 `TODO.md` 的 **B3**:三端都跑通骨架,契约文档标记 FROZEN。

## v0.2 · 2026-08-30

### 新增

- **`UI_PC.md`(1073 行)** —— PC 端 UI 规格。SPEC 原来只有 §8.3 六条,
  「组件好看、动效流畅、交互完善」在契约里完全没有落点。补齐:设计系统 / 动效规范 /
  布局与窗口 / 组件状态矩阵 / 交互(快捷键全表、按键提示、右键菜单、导航栈)/
  反馈层(toast、确认策略、错误码映射)/ 19 页逐页规格 / 播放页 OSD / 无障碍 /
  性能预算 / 验收清单。
- **`SPEC.md` §5.0 ABI 版本协商** —— 新增第 8 个导出 `lp_abi_version()`。
  理由:核心库与 UI 可被分别替换,版本错配的表现是崩溃或静默乱码,没有任何一层会喊。
- **`SPEC.md` §5.10 panic 边界** —— Go 的 panic 跨不过 cgo,三端的 catch 一个都拦不住。
  逐位置规定 recover 责任 + 三件必做事 + 先红的测试要求。
- **`SPEC.md` §5.11 事件队列与背压** —— 原来没定容量与丢弃策略。
  按事件类分级:`result`/`partial` 永不丢、高频状态事件合并、`log` 可丢但要报数。
- **`SPEC.md` §14 核心层横切规格** —— 出网(三条 UA 道 / **空闲超时不是整体超时** / 重定向一次 /
  重试归核心层)、存储(**原子写** / 磁盘配额总账)、凭据静态保护(裁决与理由)、
  本地化与时区(`err.code` 是契约、`err.msg` 是呈现;放送表按 JST 判周几)、
  单实例与深链、离线与降级、资源预算。
- 事件信封新增必需字段 `ts`(单调毫秒),用于三方时间线对账。

### 修正

- `SPEC.md` §8.1 写的「桌面 12 面板」不对,实为 **4 组 14 项**(`SettingsPage.tsx:2939` 实证)。

### 审计发现(本轮补洞的依据)

SPEC 原文对以下关键词命中为 0:`recover` / 背压 / 单实例 / locale / 本地化 / 时区 /
User-Agent / 原子 / 加密 / 配额 / 版本协商 / 无障碍 / 焦点环 / 断点 / 虚拟化 /
右键 / 快捷键 / 设计 / 动效 / 缓动。TODO 里同样没有主。

## v0.3 · 2026-08-30 · Windows / Linux 双端

### 新增

- **`SPEC.md` §15 Windows / Linux 双端差异(约 400 行)** —— 之前 §8.3 关于 Linux
  只有一句「X11 优先」。本节摊开 **20 处真实分叉**(挖自现有实现的 9 处 `cfg` 硬分叉,
  其中 6 处的注释明写「只有真跑 Linux 才现形」):
  §15.2 显示服务器裁决 / §15.3 GPU / §15.4 libmpv 加载与分发 / §15.5 系统集成 /
  §15.6 打包与系统下限 / §15.7 自更新差异 / §15.8 测试与 CI 矩阵。
- **`UI_PC.md` §3.6 双端窗口差异 + §11.3 Linux 专项验收清单。**
- **`TODO.md` §5.3b Linux 专项(L1–L11)** + **S1.4b**(Wayland 必测)+ **T18**(双端 CI 矩阵)。
- 风险登记册新增 **R9 / R10 / R11**。

### 三个改变结论的发现

1. **强制 X11 的两条理由在新架构下都失效了。** 它们是「要自己摆放顶层窗口」和
   「`mpv --wid` 在 Wayland 上不受支持」—— 视频改成窗口内合成后两条都不成立。
   Wayland 因此从「必然不行」变成「**没人验过**」,SPIKE-1 加了 S1.4b 强制双轨。
2. **双显卡钉独显在 Linux 上零实现。** Windows 侧有两个导出符号,Linux 侧什么都没有,
   「超分卡」那个故障在 Linux 上是敞开的,而且**不报错**。
3. **屏幕常亮 / 托盘 / 系统媒体控制:两端都零实现。** §8.5 把它们列为 UI 层职责,
   但那是"应该有"不是"已经有"。已在表里标 🔴。

### 修正

- **`SPEC.md` §11.3 产物表写的「Linux: AppImage 或 tar.gz」是错的。**
  必须是 **zip** —— 应用内更新器解的是 zip,喂 tar.gz 会当场失败,格式必须和更新链路一致。
- §8.5 的「屏幕常亮 / 系统媒体控制」两行补上 Linux 侧做法并标未实现。
- §7.2 surface 表补 Wayland 行(kind=5,待定)。
- §11.2 libmpv 那句补指向 §15.4(它只是一句话,展开在那一节)。

## v0.4 · 2026-08-30 · Windows 端

### 新增

- **`SPEC.md` §16 Windows 端规格(约 310 行)。** §15 回答「两端哪里不同」,
  §16 回答「**Windows 上有哪些只有 Windows 才有的坑**」—— 差异表列不出
  「UAC 虚拟化会让写入静默重定向」这种单边问题。
  §16.1 数据根三落点与探针 / §16.2 绕过数据根的暗道 / §16.3 代码签名与 SmartScreen /
  §16.4 WebView2 降级为可选 / §16.5 DPI 与窗口几何 / §16.6 Windows 文件系统语义 /
  §16.7 系统下限 / §16.8 绿色包内容断言。
- **`UI_PC.md` §3.3 重写**(四条几何陷阱 + 全屏契约 + 溢出状态)、**§11.3 Windows 专项清单**。
- **`TODO.md` §5.3a Windows 专项 W1–W12。**
- 风险登记册 **R12(未签名)/ R13(长路径与文件锁)**。

### 审计发现

补写之前,这 11 个词在 SPEC 与 UI_PC 里**命中为 0**:
SmartScreen / 代码签名 / MAX_PATH / 长路径 / 非法字符 / 保留名 / 文件锁 /
UAC / Program Files / 防火墙 / TEMP / LOCALAPPDATA / DPI 感知 / Win10。

### 三条改变做法的发现

1. **§10.1 写的「Windows = exe 同级 userdata/」是理想情况,不是全部。** 现有实现有三种落点,
   而且可写性判据**必须是真写一个探针文件再删** —— 只看"建目录成功"会被
   **UAC 虚拟化**骗过去:目录建成了,写入被悄悄重定向到 VirtualStore,
   **用户在包里找不到数据,一点错都不报**。
2. **WebView2 现在是致命依赖,而代码里没有任何缺失检测。** 新架构主 UI 是 Avalonia,
   它自然降为可选 —— 但**必须显式做,不会自动发生**:启动路径一行都不许碰它。
3. **Windows 未签名。** CI 里零签名步骤。SmartScreen + 杀软误报 + 更新后信誉归零,
   是 Windows 端第一印象的决定因素。已列三个选项与代价,**需负责人裁决**。

### 修正(我自己上一轮写错的)

- `UI_PC.md` 里我给「不要钉最大跟踪尺寸」编了一个理由(「会禁掉手动拉伸」)。
  **真实理由是:钉了会卡住原生全屏铺满整屏。** 已改。
- 同处我把 `WM_GETMINMAXINFO` 根治方案写得像是现成资产。
  **它在已经被删除的旧运行时里,当前仓库没有任何补偿代码**;
  播放窗是绕开的(几何照抄主窗、不真的 maximize)。已改成「状态未确认,SPIKE 实测」。

### 新钉住的契约

**进全屏之前必须先退出最大化。** 用户报过两次「最大化后点全屏没反应」,
挂真 exe 量出来的根因是:最大化态下切全屏,**标志位翻了而客户区一个像素都不动**
(实测 2560×1599 原样)。仓库里已有契约测试钉住这条,新架构要一并带走。

## v0.5 · 2026-08-30 · SPIKE-1a 结题(PGS/SUP × render API)

### 起因

项目负责人提出一条来自真实经历的反对意见:「之前写 Flutter 就是因为纹理导致 libmpv
一直显示不了 PGS/SUP 这类图形字幕」。**这条若成立,`SPEC.md` §7.3「默认赌 B」直接出局**
—— 图形字幕是蓝光原盘 / WEB-DL 的常态,而路径 A 的代价是 OSD 盖不上视频,
`UI_PC.md` §8 要整章重写。

### 结论:归因不成立,路径 B 未被排除 ✅

实测 6 个用例(含**自建纹理 FBO** + 真 H.264 + `d3d11va-copy` 硬解),
噪声基线**恒为 0**,字幕信号 2.2~2.8 万像素,差异区聚成 677×68 位于画面底部,
截图肉眼可辨认中英双行字幕。语料是真实 `.sup`(1359 条,1920×1080 画布)。

**交叉验证**:mpv 报的 `sub-start=532.823956 / sub-end=537.370167` 与自写 PGS 解析器
算出的该条逐位对上 —— 两个独立实现互证。

报告:[`spikes/SPIKE-1a-PGS-render-api.md`](spikes/SPIKE-1a-PGS-render-api.md),
复现脚本三个,已收进同目录。

### 顺带测出的新风险 R14 🔴

**桌面 WGL 上下文下,路径 B 拿不到 `d3d11va` 零拷贝**:
显式 `hwdec=d3d11va` **起不来**(回落软解),`auto` 最好只到 `d3d11va-copy`。

这与 `SPEC.md` §15.3 写的「Windows 默认 d3d11va 零拷贝是最佳档」**冲突** ——
那条结论来自现有 Rust 版,而它走的是路径 A(mpv 自己拥有窗口、自己建 D3D11 设备)。
已在 §15.3 加限定,并新增 TODO **S1.2b**:测 ANGLE 能否拿回零拷贝 + 量 `d3d11va-copy` 在 4K60 的代价。

### 当年 Flutter 为什么不显示(未证实)

从 git 历史取出当年实现,它建的是 **GLES 2.0** 上下文
(`EGL_CONTEXT_CLIENT_VERSION, 2` + `MPV_RENDER_API_TYPE_OPENGL_ES`),
而 mpv 的 GPU 渲染器面向 GLES 3.0+ 设计。**列为最可能原因,不写进 SPEC 当结论。**

### 实验方法本身的三个教训(已写进报告 §7)

1. mpv 事件号是 `START_FILE=6 / END_FILE=7 / FILE_LOADED=8` —— 第一版搞错,
   在文件没加载完就查属性,对照信息全是 `None`,结论不能用
2. **必须有负对照** —— 「开关字幕差 2 万像素」这个数字在没有噪声基线时什么都不证明
3. 大幅负 `sub-delay` 不要再叠 seek —— 叠了外挂字幕流跟不上,三个用例 S 全假 0,
   看起来像"硬解下 PGS 不显示"
