# SPIKE-N · <标题>

> 状态:未开始 / 进行中 / 已结题
> 负责人:
> 起止:

## 1. 要回答的问题

一句话。如果一句话说不清,说明这个 SPIKE 该拆成两个。

## 2. 为什么它排在业务代码之前

它失败会改变什么架构决策?写清楚。答案是"不会改变任何决策"的 SPIKE 不该做。

## 3. 做法

具体到能被别人重跑的程度:环境、版本、命令、代码在哪。

## 4. 实测数据

**这一节不能是空的,也不能是文字描述。** 表格、数字、截图、日志片段。

| 指标 | 期望 | 实测 | 通过? |
|---|---|---|:--:|
| | | | |

环境记录(缺一项就没法复现):

- OS / 版本:
- 硬件(CPU / GPU / 内存):
- 依赖版本(Go / libmpv / NDK / .NET / …):
- 测试素材(分辨率 / 编码 / 码率):

## 5. 结论

从数据推出来的一句话。

**禁止**出现这些措辞:"应该可以"、"理论上没问题"、"看起来行"、"暂时没发现问题"。
未验证的归因不是结论。

## 6. 若失败:备选方案

即使这次通过了也要填 —— 写下"如果三个月后它在某个环境上崩了,下一步走哪条路"。

## 7. 对 SPEC 的影响

这次 SPIKE 之后 `SPEC.md` 哪几节要改?列出来,并在改完后打勾。

- [ ] `SPEC.md` §_ ：

## 8. 遗留问题

这次没查、但和这个问题相邻的东西。别让它们静默消失。
