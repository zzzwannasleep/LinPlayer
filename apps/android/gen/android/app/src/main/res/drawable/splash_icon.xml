<?xml version="1.0" encoding="utf-8"?>
<!--
  开机那一瞬间系统闪出来的图标(仅 API 31+ 走这里,见 values-v31/themes.xml)。

  ★ 为什么要有这个文件:Android 12 起系统**强制**给每个应用画一屏 SplashScreen,
    图标默认取 `android:icon`。而 `@mipmap/ic_launcher` 是一张**满幅**的传统图标
    (没有自适应图标那 1/3 的安全边距),系统把它塞进开屏那个图标槽再放大 ——
    结果就是启动瞬间一张巨大的、糊掉的图标闪一下。

  ★ 上一版(2026-07-30)用的是 `<layer-list>` + `android:width/height="108dp"` +
    `gravity="center"`。**那个写法治不了这个病**:系统会把拿到的 drawable
    **整体缩放到填满**图标槽,layer-list 声明的 108dp 只是它自己的 intrinsic size,
    缩放之后等于没写。用户 2026-08-01 又报了一次同一个症状,
    就是因为上一版的修法从头到尾没生效过 —— 而它"看起来是修了的",
    这类改动只有真机开机那 0.4 秒能签字。

  ★ 真正管用的是**在 drawable 内部留出透明边距**,让"图标 + 边距"整体被缩放 ——
    <inset> 就是干这个的。22% 之后可见部分约占槽位的 56%,
    和自适应图标(内圈 2/3 再收一点)看起来是一档大小。
    (百分比形式的 inset 要 API 28+;这张图只被 values-v31 的主题引用,不会在低版本上被解析。)

  ★ 这里**不动 `android:icon` 本身** —— 那是桌面/抽屉里的启动器图标,
    用户没说要换。只改开屏这一处。
-->
<inset
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@mipmap/ic_launcher"
    android:inset="22%" />
