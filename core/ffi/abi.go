package main

// LP_ABI 是这份契约的版本号,**只在破坏性变更时 +1**(SPEC §5.0)。
//
// 破坏性 = 导出函数签名 / 事件外层信封 / 错误对象形状 / surface 交接语义 变了。
// 不算破坏性 = 新增命令、新增事件 name、给已有 JSON 加字段 —— 那些靠
// `system.capabilities` 探测。
//
// ★ 真值只在这一处。三端绑定由生成器写入,**不许手抄**;
//
//	契约测试断言「三端绑定里的 LP_ABI == 本常量」,不等即红。
const LP_ABI = 1
