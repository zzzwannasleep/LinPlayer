# CLAUDE.md

> **先读 [`AGENTS.md`](AGENTS.md)** —— 项目是什么、仓库结构、构建命令、红线、工作纪律全在那。
> 本文只写 Claude Code 专属的部分,不重复。

---

## 1. 语言与输出

- **用中文回答。** 代码注释也用中文(本仓库既有注释全是中文)。
- 报告结论要**带证据**:贴命令输出、行号、实测数字。"改好了"三个字不是交付。
- 说了"完成",就得是真跑过验证命令。没跑就说没跑。

## 1.5 写代码的风格

> 这一节是给我自己看的 —— `CLAUDE.md` 每次会话自动进上下文,写在别处的规范我不会去翻。
> 能被脚本查的都在 `scripts/check-style.sh` 里,推之前跑。

### 注释

- **只回答「为什么」。** 「是什么」代码自己会说,再写一遍是噪音。
- **一段注释不超过 6 行正文。** 一个 `<summary>`、一个 `<param>` 各算一段;
  `<summary>` / `<para>` 这类纯标签行不算。超了说明这段代码该拆,不是该配更多解释。
- **注释写结论,过程写 `docs/lessons/`。** 实测下来长注释十有八九是在复述 lessons ——
  代码里留「症状 → 原因 → 不要改回去」三句,细节指过去一句就够。
- **装饰符号(★ ☠ ⚠)整个文件最多 3 个。** 满屏星号等于一个重点都没有。
  不写「血的教训」「☠☠☠」这类修饰 —— 它不增加信息,只增加行数。
- 不写「待接」「暂无」「TODO 以后补」。要么做,要么删。

### 后端

- **不加没有触发路径的兜底。** 写 try/catch 之前先答:这里真会抛吗?抛了用户该看到什么?
  两个都答不上就别写。
- `catch { return; }` 必须带一句注释说明「为什么可以静默」,说不出来就不该吞。
- 不为一个实现造接口,不为一个值造配置,不为以后可能有的第二种情况造抽象。
- 错误 message 写中文 —— 用户会原样看到它。

### UI

风格刻度是**枚举,不是区间**。要用第五种圆角,先改这里,别就地写一个新值。

| | 允许的值 | 说明 |
|---|---|---|
| 圆角 | `0` `6` `10` `999` | 6=小件(按钮/输入/chip)、10=卡片面板气泡、999=胶囊圆形 |
| 间距 | `0` `2` `6` `10` `14` `18` `26` `34` `42` | Spacing / Padding / Margin 同一把尺,Thickness 按**每个分量**判 |
| 大偏移 | 超过 42 的**不许写字面数字** | 那不是间距节奏,是「让开某个东西」的高度 —— 抽成具名常量(`OsdClearance`),否则改了被让开的那个东西没人会想起来改它 |
| 颜色 | `Theme/Tokens.axaml` 的 token(`Tok.Of("Accent")`) | 写死的色号**必须带 alpha**:半透明 = 叠在画面上的遮罩,不透明的一律进 token |
| 控件 | 先翻 `Theme/Controls.axaml` 的现成 class | 第 64 条选择器如果只是第 12 条改个圆角,它不该存在 |

`BorderThickness` 不在这把尺里 —— 它是**边框宽度**不是间距,套上去会把发丝线加粗一倍。

页面继承 `PageBase`,用 `Scrolled/H1/H2/Dim`;反馈用 `Toast`,加载态用 `Skeleton`,
图片用 `Images.LoadAsync`,跳转用 `Nav` —— **不要自己再搭一套**。

---

## 2. Git

**直接提交并推送到 `main`,不开分支、不开 PR**(用户偏好)。

- `git add` **逐个文件**,禁用 `-A`。工作区常有不相关的未跟踪文件,别扫进来
- commit message 用中文,格式 `类型(范围): 说明`,照抄仓库既有风格(`git log --oneline -20` 看一眼)
- Bash 工具里写 commit message **别用 PowerShell here-string**,用 `$'...'` 或 heredoc
- 用户没让提交就别提交;提交前把改了什么说清楚

## 3. 派子 Agent

本仓库代码量大(Rust 58k + TS/CSS 47k),跨文件调研**优先派 agent**,别自己 grep 到上下文爆掉。

### 派之前

- **一次消息里并发派完**,别一个一个来
- 明确划分范围,**不要让两个 agent 读同一批文件**
- 用户没要求就别派 —— 单文件定位、已知位置的改动,自己做更快

### prompt 里必须写清

1. **具体读哪些文件**(带行数,让它知道分量)
2. **要挖出什么**(列条目,不要只说"分析一下")
3. **输出到哪个文件 + 结构**(让它直接写文件,别把内容回吐进我的上下文)
4. **每条结论带 `文件:行号` 出处**
5. **不许写"应该/可能/大概"**,读不出来就写"未确认"+说明查了哪里
6. **安全红线**:不许在输出里写真实域名/IP/账号/密码/token,一律占位符
7. **不要修改代码**(调研型 agent)
8. 提醒它:**代码里的中文注释往往写着真实故障的根因,是最高价值内容**

### ⚠️ 写入必须分段(2026-08-30 血的教训)

**在 prompt 里就写死:「每次工具调用写入的内容不超过 120 行,写完停,再写下一段。」**

那次派了 10 个 agent 调研,**连接错误打掉 14 次**,断点**全在大块写入那一刻** ——
读取阶段从不断。改成硬性 120 行分段后,同一个 agent 当场落盘 495 行。

关键是**给数字**。只说"分批写"没用 —— agent 会理解成"分两批",一批 500 行,照样断。

配套:
- 让它用 `cat >> 文件 <<'EOF'` 追加,不要整文件重写
- **禁止子 agent 再派子 agent**。嵌套是脆断的放大器,那次挂掉的两个子-子 agent 全是这么没的
- 同一个 agent **续跑超过 2 次还断,就别续了** —— 那是原地打转。
  换成范围极窄的新 agent(fresh context),或者自己收尾

### 派回来之后

- 子 agent 的结论**不要照单全收**。抽查几条出处,对不上就打回
- 它说"没找到"不等于不存在 —— 换个关键词自己再查一遍
- **它推翻你写的东西时,更要自己验一遍再改** —— 但验完属实就痛快改,别护着自己的初稿。
  那次 agent 推翻了我 SPEC 里的弹幕方案和 MIGRATION 里三条库选型,全部证实、全部已改

## 4. 经验存在哪(重要:2026-08-30 起改了)

**项目经验的正本在仓库里:`docs/lessons/`**,按领域分文件。

- 遇到"这为什么这么写"、"以前是不是踩过"的疑问,**先 grep `docs/lessons/`**,别凭记忆答
- 新学到的项目经验**写进 `docs/lessons/` 对应领域文件**,不要再往 memory 里堆
  —— 仓库文档能版本控制、能 grep、能被其它 agent 和人类看到,memory 做不到
- 写的时候保留细节:具体属性名、具体数字、具体症状。压缩成"注意 XX"等于没写
- **被推翻的内容直接删,不要留注解**(用户明确要求)。留着占地方,
  还会被下一个人当成有效结论再读一遍。删之前先确认:**仍然成立的那部分在别处有没有**
- 每条经验尽量带**失效条件** —— 「记的是快照而没记版本」是记忆库最大的坑,
  11 组「矛盾」里 8 组是这么来的(见 `docs/lessons/contradictions-resolved.md`)

**`~\.claude\projects\D--LinPlayer\memory\` 只留**跨项目有效的用户偏好与工作习惯,
不再存项目技术细节。

两处共同的纪律:记忆写的是**当时为真**的事。它提到某个文件 / 函数 / 开关,
**先确认还存在**再据此建议。

## 5. 真机自检的具体做法

`AGENTS.md` §5 讲了要做什么,这里是怎么做:

- **Windows 端**:`bash scripts/selfcheck-win.sh` —— 编核心 → 编壳 → 起假 Emby → 灌账号 → 起 exe → 截图。
  截图用 `scripts/shot-window.ps1`(先 SetProcessDPIAware,否则 PerMonitorV2 的窗口会截偏)
- **视频层**:截图截不到。用 Win32 `EnumWindows` 找窗口类量位置和可见性
- **核心层纯逻辑**:`go test`(记得 `PATH` 里带 `third_party/libmpv`,否则 cgo 起不来报 0xc0000135)
- ⚠️ **CDP / WebView2 / `__TAURI_INTERNALS__` 那一整套自检手法随 Rust 栈一起没了**。
  老经验里凡是提到它们的,只在 `git show rust-final:` 里还成立

## 6. 迁移期的特殊约定

见 [`docs/go-migration/README.md`](docs/go-migration/README.md)。

**2026-09-04 起 Rust 栈已从仓库删除**,前两条(黄金实现 / 功能冻结)随之作废 ——
`crates/` `apps/desktop` `apps/android` `ui/` 都不在了,要查旧实现走 `git show rust-final:<路径>`。

仍然成立的:

1. **新测试必须先红** —— 反向注入真 bug 看它红,不红的门禁等于没有门禁
2. **差分对账语料还在**(`core/cmd/diffcheck/corpus/`,18 条)。它们录的是黄金实现的**输出**,
   代码删了语料照样有效 —— 改 emby 相关逻辑时 `check-core.sh` 第 5 关会拿它们对账

写迁移文档时:引用的章节号、文件名必须真实存在。`docs/go-migration/` 里有交叉引用自检的做法,别留悬空引用。

## 7. 这个仓库里我常犯的错

按频次排,写在这里是为了不再犯:

| 错误 | 正确做法 |
|---|---|
| 报"编译通过"就收工 | Windows 端必须 `bash scripts/pack-win.sh` 出 exe |
| 只跑 `go build` 就推 | 推前跑 `check-core.sh` + `check-bindings.sh` |
| 改了功能只在一个入口验证 | 同一功能常有几套入口,每套都点 |
| 写"待接"/"核层暂无"这类注释 | **后端常常领先前端**。先 grep 命令表 + 读真实签名再下判断 |
| 复用一个函数前没读它 | 先读完再用。透明窗口下渲染抛错 = 一片黑,不报错 |
| 大文档硬塞 bash heredoc | 用文件写入工具 |
| 拿一台服务器的结论替另一台签字 | 各 fork 行为不同,分别验 |
| 把动画当性能问题的替罪羊 | 先量:多半是加载结构(串行 await / Promise.all 屏障) |
